{
  "name": "shopify-development-app",
  "version": "1.0.0",
  "description": "Shopify Boilerplate App based on MERN Stack to fully integrate into the Shopify App store. Going through OAuth, Billing API, Embedded App SDK providing an interface based on Polaris.",
  "main": "index.js",
  "keywords": [
    "Shopify",
    "Node",
    "Express",
    "React",
    "Polaris",
    "Passport",
    "App",
    "Liquix",
    "Boilerplate"
  ],
  "scripts": {
    "install": "(cd client && yarn) && (cd server && yarn)",
    "build": "concurrently \"cd client && yarn build\" \"cd server && yarn build\"",
    "heroku-postbuild": "yarn build",
    "start": "concurrently \"cd client && yarn start\" \"cd server && yarn start\"",
    "start:unix": "concurrently \"cd client && PORT=3000 yarn start\" \"cd server && PORT=3001 yarn start\"",
    "start:prod": "cd server && yarn start:prod"
  },
  "author": "Felix Tellmann",
  "license": "MIT",
  "dependencies": {
    "concurrently": "^3.5.1"
  },
  "engines": {
    "node": "8.9.1"
  }
}
